import { findGroup } from 'club-network-client/helpers/find-group';
import { module, test } from 'qunit';

module('Unit | Helper | find group');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = findGroup([42]);
  assert.ok(result);
});
