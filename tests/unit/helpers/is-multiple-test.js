import { isMultiple } from 'club-network-client/helpers/is-multiple';
import { module, test } from 'qunit';

module('Unit | Helper | is multiple');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isMultiple([42]);
  assert.ok(result);
});
